# خطة إضافة وحدات إسلامية جديدة إلى Athar Ayah

## الهدف

إضافة وحدتين مستقلتين، **الأذكار اليومية** و**اتجاه القبلة**، مع تحسين الصفحة الرئيسية ببطاقات مختصرة قابلة لإعادة الترتيب والإخفاء، من دون حذف أو كسر أي ميزة حالية أو تعديل النص القرآني أو البيانات الموجودة. يجب أن تبقى العربية هي اللغة الافتراضية، مع دعم إنجليزي حقيقي للواجهة ومَعانٍ إنجليزية واضحة للأذكار، مع فصل القرآن والحديث والدعاء والتدبر الشخصي في الواجهة والبيانات.

## القرارات المعمارية الأساسية

| المجال | القرار المقترح | سبب القرار |
|---|---|---|
| دمج الوحدات | توسيع `AppTab` وإضافة تبويبين جديدين، مع إبقاء التبويبات الحالية كما هي. على الشاشات الصغيرة يظهر الأذكار والقبلة داخل قائمة **المزيد** بدلاً من ازدحام شريط التنقل السفلي. | يحافظ على بنية التطبيق الحالية ويمنع كسر الروابط والتبويبات الموجودة. |
| الأذكار | مكوّن مستقل `AdhkarPage` مع مكوّنات فرعية للفئات والبطاقة والعداد والمشاركة. | يعزل المنطق الجديد عن قارئ القرآن والخواطر وخطط الحفظ. |
| المحتوى الشرعي | ملف بيانات ثابت للنسخة المعتمدة، مثل `src/data/adhkar.ts`، لا يمكن للمستخدم تعديله. كل عنصر يتطلب `sourceType` و`sourceTitle` و`sourceUrl` ومرجعاً محدداً قبل إدراجه. | يمنع تعديل النص الشرعي من الواجهة ويجعل المصدر قابلاً للمراجعة. |
| حالة المستخدم | حفظ الموضع والعداد والمفضلة في مسارات مملوكة للمستخدم داخل Firestore، مثل `users/{uid}/adhkarProgress/{itemId}` و`users/{uid}/adhkarFavorites/{itemId}`، مع تخزين مؤقت محلي للقراءة السريعة فقط. | يحقق المزامنة بين الأجهزة مع عزل بيانات كل مستخدم بقواعد UID الحالية. |
| العداد | منطق نقي قابل للاختبار: زيادة واحدة لكل تفاعل مقبول، حد أقصى يساوي عدد التكرارات، وقفل قصير لمنع النقر المزدوج غير المقصود، مع زر إعادة ضبط مؤكد. | يمنع تجاوز العدد أو مضاعفة العداد بسبب تفاعل سريع أو إعادة التصيير. |
| النص العربي | عرض النص الأصلي في عنصر مستقل `dir="rtl"` وبلا حقل تحرير أو مسار كتابة من المستخدم. المعنى الإنجليزي في عنصر مستقل `dir="ltr"` وموسوم بوضوح بأنه معنى/ترجمة. | يحافظ على سلامة النص الشرعي ويمنع الخلط بين النص والمحتوى التفسيري. |
| القبلة | صفحة `QiblaPage` تحسب الـgreat-circle initial bearing من موقع المستخدم إلى إحداثيات الكعبة الثابتة، ثم تقارنها مع heading الحساس إن توفر. | الحساب يعمل من دون خدمة خارجية، ولا يحتاج إلى تخزين الموقع أو طلب API إضافي. |
| الخصوصية | طلب الموقع فقط عند فتح صفحة القبلة، وعدم استدعاء Geolocation أو Device Orientation في `App` أو عند بدء التشغيل. لا يُحفظ الموقع الدقيق افتراضياً. | يطابق مبدأ أقل صلاحية ويمنع التتبع غير الضروري. |
| مواقيت الصلاة | خارج التنفيذ الحالي. لن تضاف بطاقة مواقيت الصلاة إلا بعد اعتماد مصدر الحساب، المدينة/الإحداثيات، المنطقة الزمنية، وطريقة الحساب وسياسة التخزين. | يمنع عرض مواقيت قد تكون خاطئة أو غير قابلة للتفسير. |
| التصميم | الحفاظ على لوحة الألوان الهادئة الحالية، وTailwind tokens، و`lucide-react` فقط للأيقونات، وتصميم Mobile First مع انتقالات قصيرة واحترام `prefers-reduced-motion`. | يضمن اتساق الهوية وعدم إدخال نظام تصميم موازٍ. |

## المرحلة 0: حماية النسخة الحالية قبل التعديل

قبل أي تغيير تنفيذي، سيتم فحص حالة Git والفرع الحالي والفروقات، ثم إنشاء نقطة رجوع محلية واضحة، مثل فرع `backup-before-islamic-modules-<date>` أو patch محفوظ خارج الملفات التي ستُعدّل. لن يُضمّن أي ملف غير مقصود، ولن تُمسح ملفات Firebase أو سجلات المستخدمين، ولن يحدث نشر تلقائي.

سيتم توثيق commit/branch الاحتياطي في سجل التنفيذ، ثم تُنفذ الإضافات على فرع مستقل. لا تُجرى أي migration مدمرة؛ الإضافات الجديدة ستبدأ بحالة افتراضية فارغة للمستخدمين الحاليين.

## المرحلة 1: تدقيق المحتوى الشرعي واعتماده

سيُبنى نموذج بيانات صارم للذكر يتضمن `id`, `category`, `arabicText`, `englishMeaning`, `repeatCount`, `sourceType`, `sourceTitle`, `sourceUrl`, `reference`, و`notes`. ستُرفض أي قطعة بيانات ناقصة المصدر أثناء الاختبار أو البناء.

سيكون نطاق النسخة الأولى **حزمة منتقاة قابلة للمراجعة** تغطي الفئات العشر المطلوبة: أذكار الصباح، أذكار المساء، أذكار بعد الصلاة، أذكار النوم، أذكار الاستيقاظ، أذكار المنزل، أذكار السفر، الاستغفار والتوبة، الرقية الشرعية، وأدعية القرآن. لن يُدّعى أنها موسوعة شاملة؛ بل ستُدرج العناصر التي تم تدقيق نصها ومرجعها فقط.

مصادر المراجعة الأساسية المقترحة هي فصل [Hisn al-Muslim في Sunnah.com](https://sunnah.com/hisn) للأذكار الواردة في السنة، وصفحة [Duas من القرآن في Quran.com](https://quran.com/duas) للأدعية القرآنية، مع تسجيل الرابط والمرجع لكل عنصر. لا يكفي وجود رابط عام؛ يجب أن يرتبط كل نص بموضع أو حديث أو آية محددة. أي اختلاف في الضبط أو الصياغة سيُراجع تحريرياً قبل إدخاله، ولن تُولد النصوص أو تُعاد صياغتها آلياً.

سيُضاف ملف توثيق مثل `CONTENT_SOURCES.md` يبين المصدر، تاريخ المراجعة، نوع النص، وملاحظات الفرق بين النص العربي والمعنى الإنجليزي. ستظهر للمستخدم بطاقة مصدر قابلة للفتح، مع تنبيه ثابت: **التدبر الشخصي ليس تفسيراً شرعياً**، والمحتوى المعروض لا يغني عن الرجوع إلى أهل العلم في المسائل الشرعية.

## المرحلة 2: تنفيذ وحدة Adhkar / الأذكار

سيتم إنشاء صفحة مستقلة مع شريط فئات مختصر، وفلتر للفئة الحالية، وبطاقة ذكر واضحة. كل بطاقة ستعرض النص العربي في مساحة قراءة هادئة، المعنى الإنجليزي عند اختيار الواجهة الإنجليزية، المصدر والمرجع، وعدد التكرارات.

سيتضمن التفاعل عداداً مرئياً من الشكل `3 / 33`، وزراً رئيسياً للعد، وزر إعادة ضبط، وأزرار التالي والسابق، وزر المفضلة، ومشاركة كصورة. عند بلوغ الحد سيظهر الإكمال بوضوح دون زيادة إضافية. ستُحفظ آخر فئة وعنصر وعدّاد ومفضلة بشكل مستقل لكل مستخدم، مع debounce/lock قصير وتحديث متفائل قابل للتراجع عند فشل Firestore.

المشاركة ستستخدم Web Share API عندما يدعم الجهاز مشاركة الملفات، وستنشئ صورة نصية منسقة للذكر عبر Canvas أو SVG مع اسم التطبيق والمصدر، من دون تضمين بيانات شخصية. في المتصفحات التي لا تدعم مشاركة الملفات سيظهر بديل واضح مثل تنزيل الصورة أو نسخ النص، مع رسالة عربية/إنجليزية مناسبة. لن يُستخدم مصدر صورة خارجي أو مكتبة أيقونات ثانية.

سيتم إضافة حالات loading وempty وoffline/error، مع إبقاء المحتوى الشرعي الثابت قابلاً للعرض دون اتصال بعد تحميل التطبيق. أما حالة المستخدم، فستُحفظ محلياً مؤقتاً وتُزامن عند عودة الاتصال، مع عدم اعتبار الحفظ المحلي بديلاً عن نجاح المزامنة السحابية.

## المرحلة 3: تنفيذ وحدة Qibla / القبلة

ستُنشأ `QiblaPage` بحيث لا تستدعي الموقع إلا عند فتحها. ستعرض البطاقة حالة الإذن، زاوية القبلة بالدرجات، وسهماً واضحاً. سيُحسب الاتجاه الجغرافي من إحداثيات المستخدم إلى إحداثيات الكعبة باستخدام صيغة bearing كروية، مع تطبيع الناتج إلى `0–360` درجة.

سيُطلب اتجاه الحساس عند الحاجة وبحسب سياسة المتصفح، خصوصاً في iOS حيث قد يتطلب `DeviceOrientationEvent.requestPermission()` تفاعلاً مباشراً من المستخدم. سيعمل زر **معايرة البوصلة** على تعليم المستخدم تدوير الجهاز، ويعرض دقة تقريبية/حالة الحساس بدلاً من ادعاء دقة مطلقة. ستُدعم أحداث absolute وغير absolute عند توفرها، مع معالجة اتجاه الجهاز وتدوير الشاشة.

إذا رفض المستخدم الموقع، أو كان الموقع غير متاح، أو لم يوجد magnetometer/Device Orientation، ستظهر رسالة بديلة مفهومة: يمكن عرض زاوية القبلة الحسابية فقط إذا توفر الموقع، أو طلب تفعيل الموقع، أو توضيح أن الجهاز لا يملك بوصلة إلكترونية. لن تُعرض قيمة اتجاه مختلقة. سيُذكر صراحة أن الدقة تعتمد على الجهاز والمعايرة والبيئة المغناطيسية.

لن يُحفظ الموقع الدقيق افتراضياً، ولن يُطلب الموقع من صفحة القرآن أو الصفحة الرئيسية. ستُراعى الألوان الداكنة، وحالات التباين، و`aria-label` للسهم والزاوية، وقابلية الاستخدام باللمس ولوحة المفاتيح.

## المرحلة 4: تحسين الصفحة الرئيسية والإعدادات

ستُضاف بطاقات صغيرة لـ: متابعة الورد، أذكار اليوم، خاطرة اليوم، القبلة، تقدم الحفظ. بطاقة مواقيت الصلاة لن تُفعل حالياً إلا كعنصر غير ظاهر أو placeholder داخلي، ولا ينبغي أن تظهر للمستخدم قبل حسم المصدر والحساب والمنطقة الزمنية.

سيكون لكل بطاقة `widgetId`, `order`, و`visible`. سيبدأ المستخدم بتخطيط افتراضي محافظ لا يزدحم، ويمكنه من الإعدادات إخفاء البطاقة أو إعادة ترتيبها. ستُحفظ التفضيلات داخل مستند مملوك للمستخدم، مع fallback محلي عند انقطاع الاتصال. ستظل البطاقات روابط إلى الوحدات الجديدة أو الشاشات القائمة، ولن تُكرر منطقاً موجوداً في قارئ القرآن أو لوحة الإنجاز.

على سطح المكتب ستظهر الوحدات الجديدة في تنقل واضح أو قائمة **المزيد** بحسب المساحة. على الهاتف سيبقى شريط التنقل السفلي مختصراً، مع قائمة المزيد التي تحتوي الأذكار والقبلة والإعدادات. ستُضاف مفاتيح i18n لكل عنوان وزر وحالة وخطأ وplaceholder، مع تحديث `document.documentElement.lang/dir` كما هو قائم حالياً.

## المرحلة 5: أمان البيانات وقواعد Firestore

سيتم إضافة قواعد مسارات جديدة مقيّدة بـ`request.auth.uid == userId` لعدادات الأذكار ومفضلاتها وتفضيلات بطاقات الصفحة الرئيسية. لن تسمح القواعد للمستخدم بإنشاء أو تعديل مجموعة المحتوى الشرعي الثابت. إن تم وضع المحتوى داخل Firestore، فسيكون للقراءة العامة فقط وممنوعاً من الكتابة للعميل؛ والأفضل إبقاؤه ضمن bundle المراجع في المستودع لتجنب مصدر بيانات قابل للتغيير من العميل.

ستُراجع `firestore.rules` عبر Firebase Emulator أو harness مناسب، مع حالات سماح ورفض لمستخدمين مختلفين، وقراءة غير مسجلة الدخول، ومحاولات تغيير `userId` أو كتابة نص ذكر. ستُراجع Storage Rules أيضاً إذا استُخدم تخزين صورة المشاركة، والأصل أن تُنشأ الصورة محلياً ولا تُرفع إلى Storage.

## المرحلة 6: الاختبارات الآلية

ستُضاف اختبارات وحدات للآتي:

| مجال الاختبار | الحالات الأساسية |
|---|---|
| بيانات الأذكار | وجود الفئات العشر، عدم وجود عنصر بلا مصدر، تكرار IDs، نص عربي غير فارغ، ومرجع لكل مصدر. |
| العداد | يبدأ من صفر، يزيد مرة واحدة، لا يتجاوز الحد، إعادة الضبط، الانتقال التالي/السابق، ورفض الضغط المكرر خلال نافذة الحماية. |
| الموضع | استعادة الفئة والعنصر والعداد بعد إعادة التحميل، وعدم خلط موضع مستخدم بآخر. |
| القبلة | نتائج bearing معروفة، التطبيع إلى 0–360، التعامل مع خط العرض/الطول، وتحويل heading إلى سهم مستقر. |
| الصلاحيات | عدم استدعاء geolocation عند تشغيل `App`، والاستدعاء عند فتح صفحة القبلة فقط، وحالات الرفض وعدم الدعم. |
| الترجمة والاتجاه | وجود مفاتيح العربية والإنجليزية، `rtl` للنص العربي، `ltr` للمعنى الإنجليزي، وعدم استخدام نصوص واجهة عربية ثابتة في الوضع الإنجليزي. |
| المشاركة | إنشاء صورة/بديل عند توفر Web Share API أو غيابه، وعدم تضمين البريد أو الموقع أو بيانات المستخدم. |
| التفضيلات | ترتيب وإخفاء البطاقات، القيم الافتراضية، وحماية التفضيلات بملكية المستخدم. |

سيتم تشغيل `npm test` فعلياً؛ وإذا لم يكن script موجوداً فسيُضاف بشكل صريح إلى `package.json` ليشغل Vitest، مع عدم إزالة الاختبارات السابقة. ثم يُشغّل `npm run lint` و`npm run build`، وتُصلح جميع الأخطاء قبل اعتبار المرحلة ناجحة.

## المرحلة 7: التحقق الوظيفي والمتجاوب

سيُختبر التطبيق في بيئة تطوير/preview بعد تسجيل الدخول بحساب Demo مخصص فقط، مع الحفاظ على عدم إدخال بيانات الحساب أو كلمة المرور في المستودع أو التقرير. ستشمل الجولة العربية والإنجليزية، الوضع الداكن، إعادة التحميل، الانقطاع والعودة من الإنترنت، إنشاء العدادات والمفضلة، المشاركة، فتح القبلة ورفض/قبول الموقع، وفتح الوحدات من البطاقات والتنقل.

سيُفحص Android Chrome وiPhone Safari على جهازين حقيقيين أو جلسات اختبار موثوقة، خصوصاً صلاحيات الموقع والبوصلة وWeb Share وPWA. إذا لم يتوفر جهاز iPhone أو Android حقيقي، ستُسجل الحالة BLOCKED بدلاً من ادعاء نجاحها. لن يُنشر أي تغيير إلى Firebase Hosting قبل مراجعة التقرير ونجاح الفحوصات المتاحة.

## المرحلة 8: التوثيق والتسليم

سيتم تحديث `QA_FINAL_REPORT.md` أو إنشاء تقرير إضافة مستقل يوضح لكل اختبار: الاسم، الخطوات، النتيجة الفعلية، PASS/FAIL/BLOCKED، الدليل أو مسار الصورة، والإصلاح المقترح. سيُضاف قسم للمحتوى الشرعي ومصادره، وقسم للخصوصية وقواعد Firebase، وقسم للمخاطر المعروفة.

سيُسلّم كذلك `CONTENT_SOURCES.md` وأي اختبارات جديدة، مع ملخص الملفات المقصودة فقط، وcommit منفصل واضح. لن يتم تنفيذ deploy تلقائي. بعد موافقة صاحبة المشروع ونجاح الاختبارات، ستكون أوامر النشر المقترحة في Cloud Shell هي سحب الفرع الصحيح، تثبيت الاعتماديات، تشغيل lint/test/build، ثم نشر hosting والقواعد فقط إذا تغيرت واختُبرت.

## معايير القبول

يُعتبر التنفيذ جاهزاً للمراجعة النهائية عندما تتحقق الشروط التالية: لا توجد إزالة أو تغييرات غير مقصودة في الميزات أو النص القرآني؛ الأذكار العشر ممثلة بمحتوى موثق غير قابل للتحرير؛ العداد والموضع والمفضلة تعملان دون تكرار غير مقصود؛ القبلة لا تطلب الموقع عند بدء التطبيق وتعرض حالات الرفض وعدم الدعم؛ البطاقات قابلة للإخفاء والترتيب دون ازدحام؛ RTL/LTR والوضع الداكن يعملان؛ قواعد Firebase تمنع تسريب بيانات المستخدم؛ وتنجح `npm run lint` و`npm run build` و`npm test`، مع توثيق اختبارات الهاتف كـPASS أو BLOCKED بصدق.

## الافتراضات والمخاطر المفتوحة

يفترض التنفيذ أن Firebase الحالي سيبقى مصدر الحسابات والبيانات، وأن التفضيلات الجديدة يمكن إضافتها كمسارات فرعية آمنة بلا migration مدمرة. جودة البوصلة لا يمكن ضمانها من المتصفح وحده، لأن بعض الأجهزة لا توفر حساساً أو تتطلب إذناً خاصاً. كما أن اعتماد نصوص الأذكار يحتاج مراجعة تحريرية بشرية لكل عنصر؛ لذلك لا يجوز ملء البيانات اعتماداً على توليد آلي أو مصدر غير محدد.

مواقيت الصلاة تبقى خارج النطاق إلى أن تُحسم طريقة الحساب ومصدر البيانات والمنطقة الزمنية وسياسة المدينة. كذلك لن تُحفظ الإحداثيات الدقيقة أو تُرسل إلى خادم جديد ضمن هذه الإضافة، ما لم توافق صاحبة المشروع لاحقاً على سياسة خصوصية واضحة.

## مراجع تقنية ومحتوى أولية

[1]: https://sunnah.com/hisn "Hisn al-Muslim على Sunnah.com"
[2]: https://quran.com/duas "أدعية القرآن على Quran.com"
[3]: https://developer.mozilla.org/en-US/docs/Web/API/DeviceOrientationEvent "MDN DeviceOrientationEvent"
[4]: https://www.w3.org/TR/orientation-event/ "W3C Device Orientation and Motion"
[5]: https://developer.mozilla.org/en-US/docs/Web/API/Navigator/share "MDN Navigator.share"
[6]: https://developer.mozilla.org/en-US/docs/Web/API/Web_Share_API "MDN Web Share API"

> هذه الخطة لا تنفذ التعديلات بعد؛ هي مقترح تنفيذ قابل للمراجعة والموافقة. لن يُنتج أو يُنشر محتوى شرعي غير موثق ضمن مرحلة التنفيذ.
