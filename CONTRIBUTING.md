# المساهمة في مشروع أثر آية (Athar Ayah)

نرحب بجميع المساهمات التي تهدف إلى تحسين وتطوير تطبيق "أثر آية" لخدمة قراء كتاب الله.
إذا كنت مطوراً أو مصمماً أو باحثاً، يمكنك مساعدتنا في تحسين هذا التطبيق.

## كيف تساهم؟

1. **الإبلاغ عن الأخطاء (Bug Reports):**
   إذا واجهت أي مشكلة، يرجى فتح `Issue` جديدة مع وصف تفصيلي للمشكلة وخطوات إعادة إنتاجها.

2. **اقتراح ميزات جديدة (Feature Requests):**
   نرحب بالأفكار الجديدة التي تخدم الهدف الأساسي للتطبيق (التدبر، الحفظ، المراجعة). افتح `Issue` لاقتراح الميزة وناقشها مع المجتمع قبل البدء في كتابة الكود.

3. **المساهمة بالكود (Pull Requests):**
   - قم بعمل Fork للمستودع.
   - قم بإنشاء فرع جديد لميزتك (`git checkout -b feature/amazing-feature`).
   - قم بعمل Commit لتغييراتك (`git commit -m 'Add some amazing feature'`).
   - ارفع التغييرات إلى فرعك (`git push origin feature/amazing-feature`).
   - افتح Pull Request ووضح فيه ما قمت به.

## معايير الكود (Code Standards)

- نستخدم `TypeScript` بشكل أساسي. يرجى التأكد من عدم وجود أخطاء في الأنواع (Type Errors) قبل فتح PR.
- نستخدم `ESLint` و `Prettier` لتنسيق الكود. قم بتشغيل `npm run lint` قبل رفع التغييرات.
- استخدم `Tailwind CSS` للتنسيق بدلاً من كتابة CSS مخصص إلا عند الضرورة القصوى.
- يرجى الحفاظ على تصميم متجاوب (Responsive) يدعم الشاشات الصغيرة والكبيرة.
- تأكد من أن التطبيق يعمل بشكل جيد في وضع الـ Offline.

## إعداد بيئة التطوير المحلية

1. استنسخ المستودع: `git clone https://github.com/Fatoomnoour/athar-ayah.git`
2. ادخل للمجلد: `cd athar-ayah`
3. ثبّت الاعتماديات: `npm install`
4. انسخ ملف المتغيرات البيئية: `cp .env.example .env.local` وقم بتعبئته ببيانات مشروع Firebase الخاص بك.
5. شغّل خادم التطوير: `npm run dev`

## حقوق الملكية والمصادر

نرجو الانتباه إلى أن نصوص القرآن الكريم، التلاوات، والترجمات يجب أن تكون من مصادر موثوقة ومفتوحة المصدر (مثل Quran.com API). لا تقم بإضافة أي محتوى محمي بحقوق نشر مغلقة.

شكراً لمساهمتك في هذا العمل الخيّر!
