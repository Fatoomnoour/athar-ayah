# QA Final Report — Athar Ayah Islamic Modules

**تاريخ الفحص:** 23 أغسطس 2026. **بيئة التنفيذ:** نسخة محلية من المشروع على `http://localhost:3000` باستخدام حساب Demo المخصص للاختبار فقط. تم استخدام النسخة المنشورة في تقارير سابقة، لكن هذا التقرير لا يعتبر Production ناجحاً بناءً على اختبار محلي، ولم يتم تنفيذ Firebase deploy في هذه المرحلة.

## ملخص الحالة

تم تنفيذ وحدات أولية قابلة للاستخدام للأذكار، والقبلة، ومواقيت الصلاة، ودمجها في التنقل والبطاقات الرئيسية. نجحت فحوصات TypeScript والبناء واختبارات Vitest بعد إضافة script رسمي باسم `test`. أُجري اختبار وظيفي محلي فعلي لتسجيل الدخول، التنقل، الأذكار، القبلة، مواقيت الصلاة، تغيير المدينة واللغة. لا تزال بعض الحالات بحاجة إلى أجهزة حقيقية أو Firebase Emulator أو مراجعة منتج قبل الإطلاق؛ لذلك **لا يوصي هذا التقرير بالنشر النهائي بعد**.

## الاختبارات التقنية

| الاختبار | الخطوات والأدلة | النتيجة الفعلية | الحالة |
|---|---|---|---|
| `npm run lint` | تشغيل الأمر من جذر المشروع بعد دمج الوحدات | انتهى بنجاح دون أخطاء TypeScript | **PASS** |
| `npm run build` | إنشاء Service Worker ثم Vite build وesbuild | انتهى بنجاح. ظهر تحذير حجم chunk كبير، لكنه لم يفشل البناء | **PASS with warning** |
| `npm test` | فشل أول تشغيل لأن `package.json` لم يعرّف script باسم `test`؛ أضيف `test: vitest run` ثم أعيد التشغيل | ملفا اختبار، و7 اختبارات ناجحة | **PASS after fix** |
| `git diff --check` | فحص المسافات الزائدة بعد التعديل | نجح بعد تنظيف المسافات | **PASS** |
| الأسرار | فحص الملفات المتتبعة بحثاً عن `.env` أو ملفات secret | لم يظهر ملف بيئة متتبع في Git. يبقى الفحص النهائي ضرورياً قبل commit | **PASS — pre-commit only** |

## الاختبارات الوظيفية المنفذة فعلياً

| اسم الاختبار | الخطوات | النتيجة الفعلية | الحالة | المشكلة أو الإصلاح المقترح |
|---|---|---|---|---|
| تسجيل الدخول | فتح التطبيق المحلي، إدخال حساب Demo، الضغط على Sign in | تم الدخول وظهرت الصفحة الرئيسية وبيانات الحساب | **PASS** | يجب إعادة الاختبار على Production بعد النشر |
| تسجيل الخروج | لم تُنفذ عملية الضغط على Sign out في هذه الجولة | لا توجد نتيجة فعلية موثقة | **BLOCKED** | تنفيذها وتوثيق عودة شاشة الدخول |
| فتح القرآن | فتح تبويب Quran ومراجعة فهرس السور والصفحات والأجزاء | ظهرت خيارات السور 1–114، والصفحات والأجزاء، وحالة تحميل الآيات | **PASS — sampled** | لم تُنفذ كل انتقالات الآيات يدوياً |
| تصنيف السور | فحص لوحة التقدم وقائمة السور | الفاتحة مكية، الأنعام مكية، الأعراف مكية، البقرة مدنية؛ وظهرت بيانات 114 سورة | **PASS — sampled** | يلزم اختبار آلي كامل للتصنيف والعدد قبل الإطلاق |
| بطاقات الصفحة الرئيسية | فتح Progress ومراجعة البطاقات | ظهرت بطاقات الصلاة القادمة، الأذكار والقبلة، وتعمل كروابط تنقل | **PASS** | بطاقتا «خاطرة اليوم» و«تقدم الحفظ» ليستا بطاقتين مستقلتين جديدتين |
| اللغة والاتجاه | التبديل إلى العربية ثم الإنجليزية ثم العودة | تغير العنوان والنصوص والاتجاه RTL/LTR | **PASS with residual intentional Arabic** | أسماء السور والنص القرآني والمحتوى العربي المقصود ليس تسريب ترجمة؛ يلزم تدقيق شامل لكل شاشة |
| الأذكار — الفئات | فتح Adhkar وفحص شريط الفئات | ظهرت الفئات العشر المطلوبة | **PASS** | البيانات الحالية Starter Dataset وليست موسوعة كاملة |
| الأذكار — العداد | الضغط على بطاقة الذكر مرة واحدة | تغير العداد من `0 / 3` إلى `1 / 3` | **PASS** | لم يُختبر النقر المزدوج السريع على جهاز لمس حقيقي |
| الأذكار — حفظ الموضع | مراجعة الكود الذي يحفظ counts وfavorites بمفتاح user id | الحفظ المحلي موجود، لكن لم يُنفذ reload مستقل أو اختبار حسابين فعليين | **BLOCKED** | إضافة اختبار UI للـ reload والفصل بين حسابين |
| الأذكار — المصدر والنص | فتح Source & Reference | ظهرت وصلة المصدر، والنص الشرعي غير قابل للتحرير | **PASS** | يجب مراجعة كل عنصر على مستوى محرر شرعي قبل التوسع |
| الأذكار — المفضلة والمشاركة | فحص القلب والمشاركة، ثم إضافة توليد PNG من المحتوى القانوني | ظهرت الأزرار، وأصبح الكود يولد صورة PNG ويدعم Web Share files أو التنزيل أو fallback النصي؛ لم تُنفذ المشاركة الفعلية على جهاز | **BLOCKED — code fix added** | تنفيذ مشاركة فعلية على Android/iPhone والمتصفحات التي لا تدعم Web Share |
| القبلة — توقيت الطلب | تشغيل التطبيق ثم فتح Qibla | لم يظهر طلب موقع عند تشغيل التطبيق؛ ظهر زر Grant Permission داخل القبلة | **PASS** |
| القبلة — رفض الموقع | الضغط على Grant Permission في بيئة الاختبار | بقيت واجهة الصلاحية ولم يُعرض اتجاه خاطئ؛ لم تُحاك كل حالات الرفض يدوياً | **PASS — limited** | اختبار denied state على Android/iPhone |
| القبلة — الحساسات | فحص fallback البرمجي ورسالة no-compass | الكود يحتوي fallback عند غياب heading | **BLOCKED** | يلزم جهاز حقيقي يدعم ولا يدعم الحساسات |
| مواقيت الصلاة — المصدر | فتح Prayer Times | ظهرت الفجر والشروق والظهر والعصر والمغرب والعشاء ومصدر الحساب | **PASS** | المصدر AlAdhan API، ولا توجد مواقيت ثابتة في الواجهة |
| مواقيت الصلاة — الافتراضي | فتح الصفحة مع عدم وجود cache | ظهرت Cairo, Egypt وطريقة Egyptian General Authority of Survey | **PASS** |
| مواقيت الصلاة — تغيير المدينة | إدخال Riyadh وSaudi Arabia، الحفظ ثم Update Times | تغيرت المدينة والمواقيت والعداد إلى Riyadh | **PASS** |
| مواقيت الصلاة — الصلاة القادمة | مراقبة البطاقة والعداد | ظهرت Maghrib والعداد تناقص مع مرور الوقت | **PASS** |
| مواقيت الصلاة — التخزين وإعادة التحميل | مراجعة localStorage بعد تغيير المدينة | الكود يحفظ settings وdata محلياً؛ لم ينفذ reload مستقلاً في هذه الجولة | **BLOCKED** | تنفيذ reload وDelete Saved City قبل الإطلاق |
| إشعارات الصلاة | فحص زر Enable Alerts | الطلب لا يحدث إلا بعد ضغط المستخدم، لكن لا توجد جدولة خلفية مكتملة | **PASS — permission only** | عدم تقديمها كتنبيهات عاملة حتى تنفيذ scheduler/FCM موثق |
| الوضع الداكن | فحص classes للوحدات الجديدة | الوحدات تحتوي dark classes؛ لم تُنفذ جولة كاملة لكل الصفحات | **BLOCKED** | اختبار بصري شامل |
| Android Chrome | PWA، الحساسات، الصوت والتخزين | لا يوجد جهاز Android متصل | **BLOCKED** | اختبار على جهاز Android حقيقي |
| iPhone Safari | DeviceOrientation permission وWeb Share وPWA | لا يوجد جهاز iPhone متصل | **BLOCKED** | اختبار على iPhone Safari حقيقي |
| Firebase Security Rules | مراجعة القواعد بحثاً عن grants عامة | لا توجد `allow read/write: if true`، والمسارات الجديدة owner-scoped | **BLOCKED — code review only** | تشغيل Firebase Emulator لاختبار user A مقابل user B وصلاحيات المجموعات |
| تسريب بيانات المستخدم | مراجعة مفاتيح التخزين المحلي | counts وfavorites للأذكار مرتبطة بـ`currentUser.id`؛ مواقيت المدينة تفضيل محلي عام | **PASS — code review** | اختبار حسابين فعليين ومسح الحالة وفق سياسة logout |

## التغييرات والإصلاحات الرئيسية

تم إنشاء فرع التطوير `feature/islamic-modules` ونسخة patch احتياطية قبل التعديل. أضيفت ملفات بيانات مستقلة للأذكار ومواقيت الصلاة، ومكونات `AdhkarPage` و`QiblaPage` و`PrayerTimesPage`، وخدمة AlAdhan، واختبارات Vitest. أضيفت مفاتيح الترجمة والتنقل والبطاقات إلى التطبيق، وأضيفت قواعد owner-scoped لمسارات `adhkarProgress` و`adhkarFavorites` و`prayerSettings`. كما استُعيد `functions/package-lock.json` و`public/firebase-messaging-sw.js` بعد اكتشاف تغييرات خارج النطاق.

تم تصحيح بيانات الأذكار لتجنب النصوص المبتورة بعلامة الحذف، وتغطية الفئات العشر بعناصر Starter Dataset ذات مراجع مباشرة. تظل النصوص الشرعية في ملف بيانات غير قابل للتحرير من المستخدم، مع إظهار المصدر والتنبيه بأن التدبر الشخصي ليس تفسيراً شرعياً.

## المصادر

تستخدم مواقيت الصلاة واجهة AlAdhan، مع افتراضي طريقة الحساب المصرية رقم 5. التوثيق في [AlAdhan Prayer Times API](https://aladhan.com/prayer-times-api) و[AlAdhan Calculation Methods](https://aladhan.com/calculation-methods).

تمت مراجعة عناصر الأذكار الأولية من صفحات مباشرة في Sunnah.com، ومنها [Hisn al-Muslim 87](https://sunnah.com/hisn:87)، و[Sahih al-Bukhari 6312](https://sunnah.com/bukhari:6312)، و[Sahih Muslim 597a](https://sunnah.com/muslim:597a)، و[Sahih Muslim 2018a](https://sunnah.com/muslim:2018a)، و[Sahih Muslim 1342](https://sunnah.com/muslim:1342)، و[Sahih al-Bukhari 6306](https://sunnah.com/bukhari:6306)، مع [Sahih al-Bukhari 5736](https://sunnah.com/bukhari:5736) لسياق الفاتحة في الرقية. الدعاء القرآني موثق عبر [Quran.com 2:201](https://quran.com/2/201) و[Quran.com Al-Fatihah](https://quran.com/1).

حساب القبلة يعتمد على bearing كروي إلى إحداثيات الكعبة الثابتة في الكود، بينما الوصول إلى الموقع والحساسات يحتاج موافقة المستخدم. دعم الحساسات يعتمد على [MDN DeviceOrientationEvent](https://developer.mozilla.org/en-US/docs/Web/API/DeviceOrientationEvent)، والمشاركة على [MDN Navigator.share](https://developer.mozilla.org/en-US/docs/Web/API/Navigator/share).

## المتبقي قبل الإطلاق

تنبيهات الصلاة المجدولة في الخلفية، حذف المدينة المحفوظة من واجهة واضحة، حفظ تقدم الأذكار في Firestore، اختبار reload، اختبار تسجيل الخروج، اختبار مشاركة PNG الفعلية على الأجهزة، اختبار Android/iPhone، واختبار Firebase Emulator غير مكتملة أو غير منفذة. كما أن مجموعة الأذكار الحالية إصدار أولي وليست محتوى شاملاً لكل الأبواب. يجب معالجة هذه النقاط أو اعتمادها رسمياً كقيود إصدار قبل Pull Request أو النشر.

## أوامر التحقق

```bash
cd ~/athar-ayah
npm run lint
npm run build
npm test
git diff --check
git status --short
```

قبل commit، أضف الملفات المقصودة فقط، وتأكد من عدم وجود `.env` أو حذف غير مقصود لـ`functions/package-lock.json`. لا ينفذ Production deployment إلا بعد نجاح الاختبارات المحجوبة وإعادة اختبار الرابط المنشور.
